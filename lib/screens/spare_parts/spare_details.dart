
import 'package:flutter/material.dart';

class SpareDetail extends StatelessWidget {
  // final int index;
  // SpareDetail(this.index);
  // SpareDetail({this.assetPath, this.cookieprice, this.cookiename});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}